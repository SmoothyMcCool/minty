doctype html
html(lang='en')

	head
		meta(charset='UTF-8')
		meta(
			name='viewport'
			content='width=device-width, initial-scale=1'
		)
		link(
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
			onerror="this.onerror=null;this.href='Minty/bootstrap.min.css';"
		)

		style.
			.pre {
				white-space: pre-wrap;
				margin: 0;
			}

			.rotate {
				display: inline-block;
				transition: transform 0.3s ease;
			}

			.rotate.down {
				transform: rotate(90deg);
			}

	body

		h1 Review Results

		h2 Reviewed Code
		span.text-primary(
			data-bs-toggle="collapse"
			data-bs-target="#reviewedCode"
			role="button"
			aria-expanded="false"
			onclick='this.querySelector(".rotate").classList.toggle("down")'
		)
			| Show Details
			span.rotate.ms-2 ▸

		div.collapse.mt-3(id="reviewedCode")
			.card.card-body
				pre
					code!= results[0][0]

		h2 Analysis
		#{results[1][0]}

		script(
			src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
			onerror="this.onerror=null;this.src='Minty/bootstrap.bundle.min.js';"
		)
